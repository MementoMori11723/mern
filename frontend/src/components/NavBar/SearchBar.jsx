import React from "react";
import { Link } from "react-router-dom";

const SearchBar = () => {
  return (
    <Link to="/search">
      <h1>SearchBar </h1>
    </Link>
  );
};

export default SearchBar;
